from setuptools import setup, find_packages

setup(name='python_svg_parser',
      version='0.0.1',
      description='Python SVG parser forked from cjlano/svg',
      url='https://github.com/allenai/svg',
      packages=find_packages(),
      )
